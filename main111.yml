---
  - name: Initial Playbook
    hosts: all
    gather_facts: no
    connection: network_cli
    tasks:
      - name: run the command 'show version'
        provider: "{{ cli }}"
        ios_command:
          commands: show version
